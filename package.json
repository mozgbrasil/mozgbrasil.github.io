{
  "name": "@mozgbrasil/mozgbrasil-github-io",
  "version": "1.0.0",
  "private": true,
  "description": "Public landing, portfolio and GitHub Pages identity surface for the MOZG laboratory.",
  "homepage": "https://mozg.com.br/",
  "directories": {
    "doc": ".",
    "test": "tests"
  },
  "config": {
    "publicSite": "https://mozg.com.br/",
    "publicProfile": "https://github.com/mozgbrasil",
    "publicPortal": "https://mozgbrasil.github.io/"
  },
  "bugs": {
    "email": "mozgbrasil@gmail.com",
    "url": "https://mozg.com.br/contato"
  },
  "scripts": {
    "matrix:check": "node scripts/matrix-check.js",
    "format:check": "bash scripts/build.sh format-only",
    "lint": "bash scripts/build.sh lint-only",
    "test": "bash scripts/build.sh test-only",
    "check": "bash scripts/build.sh check-only",
    "check:full": "bash scripts/build.sh all",
    "surface:json": "node scripts/site-surface.js --format=json",
    "surface:md": "node scripts/site-surface.js --format=md",
    "surface:ready": "node scripts/site-surface.js --view=readiness --format=json",
    "surface:links": "node scripts/site-surface.js --view=links --format=json",
    "surface:links:ndjson": "node scripts/site-surface.js --view=links --format=ndjson",
    "ci": "bash scripts/build.sh check-only"
  },
  "author": "Mozg Brasil",
  "license": "MIT"
}
